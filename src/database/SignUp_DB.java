package database;

public class SignUp_DB {


    //     Class.forName("Driver Name ");
    //Connection con = DriverManager.getConnection("", "", "");

  /* public void insertUserDetails(SignUpUserDetails userDetails) {
        try {

            //replace original query into the this statement
            PreparedStatement stmt = con.prepareStatement("insert into dummy values(?,?,?,?);");
            //set details according to table fields type
            stmt.setString(1, userDetails.getUserName());
            stmt.setString(2, userDetails.getUserName());
            stmt.setString(3, userDetails.getUserName());
            stmt.setString(4, userDetails.getUserName());
            stmt.execute();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }*/
  /*public void updateOTP(int OTP){
      try{
          PreparedStatement stmt = con.prepareStatement("update query(?,?,?,?);");
          //set details according to table fields type
          stmt.setString(1, OTP);
          stmt.execute();
      }catch (Exception e){e.printStackTrace();}
  }*/
}
